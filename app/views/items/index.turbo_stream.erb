<%= turbo_stream.update('items-list') do %>
  <% if @items.present? %>
    <%= render 'items/list', items: @items,
      paginate: local_assigns[:paginate],
      name: local_assigns[:name],
      offset: local_assigns[:offset]
    %>
  <% else local_assigns[:name] %>
    <p><%= t '.filter.no-results' %></p>
  <% end %>
<% end %>
