<div class="item" data-controller="items"
  data-item-id="<%= item.id %>"
  data-items-global-outlet="#global"
>
  <%= button_tag class: 'card-button', type: 'button',
    data: {
      action: 'items#show',
      'show-item-path': user_item_path(@user, item)
    } do
  %>
    <%= render 'items/pictogram', item: item %>
  <% end %>
</div>
